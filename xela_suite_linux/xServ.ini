[CAN]
bustype = socketcan
channel = can0
[viz]
max_offset = 100
max_size = 100
arrows = off #use `half` or `full` to turn on
grid = on #use `off` to turn off
transparency = off #use `on` to turn on
origins = off #use `on` to turn on
[debug]
sens_print = full
[sensor]
num_of_sensor = 1
model = uSMu
id = 2
calibration = off

